<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pjt.member.mapper.MemberMapper">

	<update id="update" parameterType="com.pjt.member.commend.MemberVO">
		update tbl_user set user_phone=#{phone}, user_address=#{address}, user_email=#{email}, user_nickname=#{nick}
		where user_id=#{id}
	</update>
	
	<update id="updatePW">
		update tbl_user set user_pw=#{newpw} where user_id=#{id}
	</update>
	
	<delete id="delete">
		delete from tbl_user where user_id=#{id}
	</delete>
</mapper>